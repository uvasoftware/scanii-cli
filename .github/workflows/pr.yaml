name: PR Audit

on:
  pull_request:
    branches: [ main ]

jobs:

  audit:
    runs-on: ${{ matrix.os }}
    strategy:
       matrix:
          os: [ ubuntu-latest, windows-latest , macOS-latest]
    steps:
      - uses: actions/checkout@v2

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.21

      - name: Audit
        run: make audit

      - name: Build
        run: make build

      - name: Run tests
        run: make test